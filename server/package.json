{
  "name": "mobile-screenshotter-server",
  "version": "1.0.0",
  "description": "Local server for mobile device screenshot capture via ADB (Android) and libimobiledevice (iOS)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "image-size": "^2.0.2"
  }
}
